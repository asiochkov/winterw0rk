# Divergence report — v7 against the app

Generated by `tools/divergence-report.mjs`. v7 carries **25 screens**
and **174 onClick handlers**.

`built from` is whichever prototype the stylesheet's own comments name.

- `v6` — transcribed, but before the reference changed. It now has v7's
  colours under a v6 layout, and has to be transcribed again.
- `unmarked` — never transcribed from any prototype. This is the app's
  own original implementation and has to be built from v7 outright.
- `none` — no stylesheet at all; the screen does not exist yet.

Read `built from` per stylesheet, not per screen: several screens share
one sheet, so a screen inherits its neighbours' marking. Exercise and
Plan detail are the ones that flatter themselves this way — they share
training.css and programs.css but were never transcribed.

Handler counts are `onClick` attributes in the markup. They undercount:
the prototype binds most behaviour through its state object, and
`01_product_inventory.json` is the complete list.

| v7 screen | lines | handlers | stylesheet | built from |
| --- | ---: | ---: | --- | --- |
| Planner | 258 | 31 | planner.css | unmarked |
| Active session | 144 | 24 | training.css | v6 |
| Training | 188 | 23 | training.css | v6 |
| Today | 214 | 17 | today.css | v6 |
| Habits | 90 | 9 | habits.css | v6 |
| Sign in | 29 | 6 | auth.css | unmarked |
| Mood | 84 | 6 | mood.css | unmarked |
| Settings | 76 | 6 | — | **none** |
| Exercise | 100 | 6 | training.css | v6 |
| Onboarding | 73 | 5 | onboarding.css | unmarked |
| Habit detail | 84 | 5 | habits.css | v6 |
| Focus | 49 | 4 | focus.css | unmarked |
| Fitness setup | 34 | 4 | onboarding.css | unmarked |
| Create account | 30 | 3 | auth.css | unmarked |
| Quit counter | 90 | 3 | quit.css | v6 |
| Profile | 66 | 3 | — | **none** |
| Nutrition | 90 | 3 | nutrition.css | unmarked |
| Street | 77 | 3 | street.css | unmarked |
| Welcome | 41 | 2 | auth.css | unmarked |
| Forgot password | 14 | 2 | auth.css | unmarked |
| Progress | 212 | 2 | progress.css | v6 |
| Program detail | 33 | 2 | programs.css | unmarked |
| Plan detail | 30 | 2 | programs.css | unmarked |
| Session summary | 45 | 2 | training.css | v6 |
| Focus history | 27 | 1 | focus.css | unmarked |

Router paths currently registered (36): `/`, `/sign-in`, `/sign-up`, `/forgot-password`, `/reset-password`, `/terms`, `/privacy`, `/onboarding`, `/today`, `/more`, `/habits`, `/habits/new`, `/habits/:id`, `/quit`, `/quit/new`, `/quit/:id`, `/training`, `/training/library`, `/training/exercises/:id`, `/training/session/:id`, `/training/session/:id/summary`, `/mood`, `/focus`, `/focus/history`, `/programs`, `/programs/:id`, `/body`, `/nutrition`, `/street`, `/progress`, `/planner`, `/steps`, `/profile`, `/settings`, `/admin`, `*`
